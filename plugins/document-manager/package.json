{
  "name": "@bizforge/plugin-document-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo \"No tests configured for document-manager\"",
    "test:watch": "echo \"No watch tests configured for document-manager\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for document-manager\""
  },
  "dependencies": {
    "@bizforge/plugin-sdk": "file:../../packages/plugin-sdk"
  }
}
