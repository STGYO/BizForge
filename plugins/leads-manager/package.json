{
  "name": "@bizforge/plugin-leads-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo \"No tests configured for leads-manager\"",
    "test:watch": "echo \"No watch tests configured for leads-manager\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for leads-manager\""
  },
  "dependencies": {
    "@bizforge/plugin-sdk": "file:../../packages/plugin-sdk"
  }
}
